package capstone

// SolveDay4Part1 returns the total distance of every budget route in
// the timetable. Each input line describes one one-way sleigh route in
// exactly this shape:
//
//	route R-17: GLACIER-GATE => TINSEL-TOWN | distance 42 | toll 7
//
// (id, origin => destination, distance in miles, toll in candy canes;
// station names are single hyphenated tokens with no spaces). A route
// is a budget route when its toll is at most 5. Parse every line —
// Part 2 needs the endpoints, so build a real struct rather than
// fishing out two numbers with string tricks.
func SolveDay4Part1(input string) int {
	// TODO: implement
	return 0
}

// SolveDay4Part2 returns the smallest number of routes a sled must
// take to travel from station NORTH-POLE to station SANTAS-WORKSHOP.
// Routes are strictly one-way: a route A => B cannot be ridden from B
// to A. Tolls and distances are irrelevant here — only the hop count
// matters. If the workshop cannot be reached at all, return -1.
func SolveDay4Part2(input string) int {
	// TODO: implement
	return 0
}
