package capstone

// SolveDay2Part1 counts the decorated trees in the grove. The input
// is a rectangular grid, one row per line, containing only '.' (open
// snow), 'T' (fir tree), and 'L' (lantern). A tree counts as
// decorated when at least one of its neighbors — up to eight,
// including diagonals; fewer on edges and corners — is a lantern.
func SolveDay2Part1(input string) int {
	// TODO: implement
	return 0
}

// SolveDay2Part2 counts the distinct open-snow '.' cells that are lit.
// Every lantern shines a beam in each of the four cardinal directions
// (up, down, left, right — never diagonally). A beam lights every
// consecutive '.' cell and stops as soon as it reaches a cell that is
// not '.' (a tree or another lantern; that blocking cell is NOT lit)
// or falls off the edge of the grid. A snow cell lit by several beams
// still counts exactly once. Lantern cells themselves are never
// counted.
func SolveDay2Part2(input string) int {
	// TODO: implement
	return 0
}
