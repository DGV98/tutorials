// Package capstone is module 17: four original Advent-of-Code-style
// puzzles. Each day has a Part1 and a Part2 that share the same raw
// input text; the full puzzle stories live in README.md. Solve the
// tiny examples in capstone_test.go first, then the real inputs.
package capstone

// SolveDay1Part1 returns the grand total of candy canes moved in the
// whole ledger. Each input line is one shift report: an elf's name
// followed by one or more space-separated signed integers (deliveries
// are positive, snacking incidents negative). The input ends with a
// newline; there are no blank lines in between. Sum every integer on
// every line. The result may be negative.
func SolveDay1Part1(input string) int {
	// TODO: implement
	return 0
}

// SolveDay1Part2 returns the name of the elf with the best personal
// total. An elf may appear on many lines; their personal total is the
// sum of every integer on every line bearing their name (and it may
// be negative). Exactly one elf has the strictly largest total.
func SolveDay1Part2(input string) string {
	// TODO: implement
	return ""
}
